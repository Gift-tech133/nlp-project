# nlp-project
# initialize git repo
git init

# add all files
git add -A

# commit files
git commit -m "Initial commit: NLP concepts demo project"

# rename default branch to main
git branch -M main

# add remote (replace <username> with your GitHub username)
git remote add origin https://github.com/<username>/nlp-demo-project.git

# push to GitHub
git push -u origin main
